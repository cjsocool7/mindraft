# Changelog

## [Unreleased]

### Added
- `Node` model with id, label, x, y fields using dataclass and UUID
- `Edge` model with id, source_id, target_id fields using dataclass and UUID
- `Graph` model as single source of truth for all nodes and edges